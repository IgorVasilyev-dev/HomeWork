package HomeWork1;

public interface ICommunicationPrinter {
    /**
     * Метод сравнивает принимает параметр name, и возвращает строку привествия
     * @param name строка
     * @return возвращает строку привествия
     */
    String welcome(String name);
}
